{
  "name": "my-soccer-coach",
  "version": "1.0.0",
  "description": "My Soccer Coach app",
  "main": "app.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp app.html dist/index.html && cp -r js img icons styles.css manifest.json sw.js dist/"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  }
}
